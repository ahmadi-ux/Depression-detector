// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
import { getFirestore } from "firebase/firestore";
import { getStorage } from "firebase/storage";
import { getAuth } from "firebase/auth";

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyAXd9LOJ-fN1Yelxk84LzOaWllGVL2p6Ko",
  authDomain: "depression-detector-d83bb.firebaseapp.com",
  projectId: "depression-detector-d83bb",
  storageBucket: "depression-detector-d83bb.appspot.com",
  messagingSenderId: "526814075286",
  appId: "1:526814075286:web:73d8ed3799cf29c94325b7",
  measurementId: "G-RXEL55YG83"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

// Initialize analytics with error handling for ad blockers had to do this on my laptop
try {
  getAnalytics(app);
} catch (error) {
  console.warn("Analytics blocked by browser extension:", error);
}

export const db = getFirestore(app);
export const auth = getAuth(app);
export const storage = getStorage(app);