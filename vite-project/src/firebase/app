// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
import { getFirestore } from "firebase/firestore";
import { getAuth } from "firebase/auth";

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyDppug1IvtGim0cu-3nV6L4aKb-JxpNHlI",
  authDomain: "personal-website-5f602.firebaseapp.com",
  projectId: "personal-website-5f602",
  storageBucket: "personal-website-5f602.firebasestorage.app",
  messagingSenderId: "348918549058",
  appId: "1:348918549058:web:fb10582d38831cc908fbdb",
  measurementId: "G-7D1BTWYZ08"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

// Initialize analytics with error handling for ad blockers had to do this on my laptop
try {
  getAnalytics(app);
} catch (error) {
  console.warn("Analytics blocked by browser extension:", error);
}

export const db = getFirestore(app);
export const auth = getAuth(app);